-- /fact — случайный «факт» (развлечение)
local facts = {
    "Первый баг нашли в 1947 году — буквально жук в реле Mark II.",
    "В Lua индексы массивов начинаются с 1. Да, именно так.",
    "TCP гарантирует порядок, но не гарантирует, что дедлайн будет соблюдён.",
    "Преждевременная оптимизация — корень всех зол (с) Д. Кнут, но профилировать всё равно надо.",
    "UTF-8 — это не «один символ = один байт», и это прекрасно усложняет жизнь.",
    "Если сборка зелёная, а прод падает — поздравляю, у вас интеграционные тесты.",
    "Регулярные выражения: у вас была проблема, теперь у вас две проблемы.",
    "git blame показывает автора строки, а не того, кто виноват в баге (обычно).",
}

RegisterCommand("fact", function(ctx)
    local line = facts[math.random(1, #facts)]
    ctx:SendSystemMessage("📚 " .. line)
end)
